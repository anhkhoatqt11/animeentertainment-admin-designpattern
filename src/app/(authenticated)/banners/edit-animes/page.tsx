import { EditAnimeBanner } from "../(components)/EditAnimeBanner";

export default function page({ }) {
    return (
        <div className="w-full h-full bg-slate-100 p-6">
            <EditAnimeBanner />
        </div>
    )
}